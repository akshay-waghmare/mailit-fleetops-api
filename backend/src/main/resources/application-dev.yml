spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fleetops_dev
    username: fleetops
    password: fleetops
    
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        
logging:
  level:
    com.fleetops: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
